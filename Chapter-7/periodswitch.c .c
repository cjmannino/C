/*periodswitch.c -- replaceds punctuations marks */
#include <stdio.h>
#include <ctype.h>
#define REPLACECHAR '!'
#define PERIOD '.'

int main(int argc, char const *argv[])
{
	char ch, holdCh;
	signed int charCount = 0; 

	printf("Start entering some sentences (use @ to quit)\n");

	while ((ch = getchar()) != '@')
	{
		if (ch((ispunct) == Period))
		{
		  printf("Bitch!\n")
		}
	}
	 printf("%c", ch);

	return 0;
}